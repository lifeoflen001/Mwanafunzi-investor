@extends('layouts.account')
@section('title', 'Order history — Mwanafunzi Investor')
@section('portal-heading', 'Orders')
@section('content')<section class="page-hero"><div class="container"><p class="eyebrow">Account / Orders</p><h1>Order history.</h1>@include('account.partials.nav')</div></section><section class="platform-section"><div class="container account-table"><div class="account-table-head"><span>Reference</span><span>Date</span><span>Total</span><span>Status</span></div>@forelse($orders as $order)<a class="account-table-row" href="{{ route('account.orders.show', $order) }}"><strong>{{ $order->order_number }}</strong><span>{{ $order->created_at->format('M j, Y') }}</span><span>{{ app(\App\Services\MoneyFormatter::class)->format($order->total, $order->currency) }}</span><span>{{ str_replace('_', ' ', $order->status->value) }}</span></a>@empty<div class="empty-state"><p>No orders yet.</p></div>@endforelse{{ $orders->links() }}</div></section>@endsection
