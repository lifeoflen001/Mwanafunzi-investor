@extends('layouts.public')
@section('title', 'Your account — Mwanafunzi Investor')
@section('content')<section class="page-hero account-hero"><div class="container"><p class="eyebrow">Student of Money portal</p><h1>Welcome, {{ auth()->user()->name }}.</h1><p class="lede">A calm place for the products and courses you have chosen.</p>@include('account.partials.nav')</div></section><section class="platform-section"><div class="container account-grid"><div><p class="eyebrow">Recent orders</p><div class="account-list">@forelse($orders as $order)<a class="account-card" href="{{ route('account.orders.show', $order) }}"><strong>{{ $order->order_number }}</strong><span>{{ $order->created_at->format('M j, Y') }} · {{ app(\App\Services\MoneyFormatter::class)->format($order->total, $order->currency) }}</span></a>@empty<div class="empty-state"><p>No orders yet.</p></div>@endforelse</div><a class="text-link" href="{{ route('account.orders') }}">View order history →</a></div><div><p class="eyebrow">Course access</p><div class="account-list">@forelse($enrollments as $enrollment)<a class="account-card" href="{{ route('account.courses.show', $enrollment) }}"><strong>{{ $enrollment->course->title }}</strong><span>{{ str_replace('_', ' ', $enrollment->status->value) }}</span></a>@empty<div class="empty-state"><p>No course enrollments yet.</p></div>@endforelse</div><a class="text-link" href="{{ route('account.courses') }}">View my courses →</a></div></div></section>@endsection
