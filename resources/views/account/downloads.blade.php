@extends('layouts.account')
@section('title', 'My tools — Mwanafunzi Investor')
@section('portal-heading', 'My tools')
@section('content')<section class="page-hero"><div class="container"><p class="eyebrow">Account / My tools</p><h1>Your downloads.</h1>@include('account.partials.nav')</div></section><section class="platform-section"><div class="container account-grid">@forelse($entitlements as $entitlement)<div class="detail-card"><p class="eyebrow">{{ $entitlement->product->name }}</p><h2>{{ $entitlement->version?->version ?: $entitlement->product->version ?: 'Purchased version' }}</h2><p>Access is {{ $entitlement->status->value }}.</p>@forelse($entitlement->product->assets as $asset)<a class="button button-dark" href="{{ $downloads->url(auth()->user(), $asset) }}">Download {{ $asset->name }} <span aria-hidden="true">↗</span></a>@empty<div class="empty-state compact"><p>The downloadable file is being prepared.</p></div>@endforelse</div>@empty<div class="empty-state"><p>Your purchased tools will appear here after a confirmed payment.</p></div>@endforelse</div></section>@endsection
