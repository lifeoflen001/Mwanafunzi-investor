@extends('layouts.public')
@section('title', 'Learn — Mwanafunzi Investor')
@section('description', 'Structured learning paths for forex foundations, mechanical analysis, risk planning and running trade management.')
@section('content')
<x-public-hero class="public-page-hero" eyebrow="Start with a stronger foundation" title="Learn to build a process that can hold up under uncertainty." summary="Four practical entry points for replacing noise with a calm, structured way of thinking about markets." setting="hero_learn_image" :fallback-image="config('public.hero_defaults.learn')" />
<section class="platform-section"><div class="container"><div class="learning-path">@forelse($topics as $topic)<article class="learning-step"><div class="step-marker">{{ str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</div><div><p class="eyebrow">{{ $topic->skill_level }} <span class="eyebrow-dot"></span> {{ $topic->study_time }}</p><h2><a href="{{ route('courses') }}">{{ $topic->title }}</a></h2><p>{{ $topic->short_description }}</p><a class="text-link" href="{{ route('courses') }}">Explore the path <span aria-hidden="true">→</span></a></div></article>@if(!$loop->last)<div class="path-arrow" aria-hidden="true">↓</div>@endif @empty<div class="empty-state"><span class="empty-index">LEARNING PATHS</span><h2>The first lessons are being prepared.</h2><p>Check back soon for the first published learning path.</p></div>@endforelse</div></div></section>
<section class="platform-dark"><div class="container two-column"><div><p class="eyebrow eyebrow-light"><span class="eyebrow-line"></span> The principle</p><h2>Always a <em>mwanafunzi.</em></h2></div><div><p>Learning is not a race to certainty. It is the practice of making better decisions with the information and risk in front of you.</p><a class="button button-accent" href="{{ route('student-of-money') }}">Read the philosophy <span aria-hidden="true">↗</span></a></div></div></section>
@endsection
