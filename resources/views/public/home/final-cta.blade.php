<section class="final-cta" id="contact"><div class="container final-cta-inner reveal"><p class="eyebrow eyebrow-light"><span class="eyebrow-line"></span> {{ $section?->payload['eyebrow'] ?? '' }}</p><h2>{{ $section?->heading ?? '' }}</h2><p>{{ $section?->body ?? '' }}</p><div class="hero-buttons">@if($section?->cta_label && $section?->cta_url)<a class="button button-accent" href="{{ $section->cta_url }}">{{ $section->cta_label }} <span aria-hidden="true">↗</span></a>@endif @if($section?->payload['secondary_cta_label'] && $section?->payload['secondary_cta_url'])<a class="text-link text-link-light" href="{{ $section->payload['secondary_cta_url'] }}">{{ $section->payload['secondary_cta_label'] }} <span aria-hidden="true">→</span></a>@endif</div></div></section>
