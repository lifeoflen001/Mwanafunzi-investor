<section class="section products-section" id="tools"><div class="container"><div class="section-heading split-heading reveal"><div><p class="eyebrow"><span class="eyebrow-line"></span> {{ $section?->payload['eyebrow'] ?? 'Tools for deliberate practice' }}</p><h2>{{ $section?->heading ?? 'Make the process visible.' }}</h2></div><p class="body-copy">{{ $section?->body ?? 'Simple, focused tools for turning a good intention into a record you can learn from. Product availability and pricing can be configured as the catalogue grows.' }}</p></div><div class="product-grid">@forelse($products as $product)@php $mockups = ['journal-mockup', 'simulator-mockup', 'risk-mockup', 'calculator-mockup']; $mockup = $mockups[$loop->index] ?? 'simulator-mockup'; @endphp<a class="product-card {{ $loop->first ? 'product-featured' : '' }} {{ $loop->iteration === 4 ? 'product-wide' : '' }} reveal" href="{{ route('tools.show', $product) }}"><div class="product-mockup {{ $mockup }}"><div class="mockup-top"><span>{{ strtoupper($product->name) }}</span><span>{{ str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</span></div><div class="sim-chart"><i></i><i></i><i></i><i></i><i></i><i></i><i></i></div><div class="risk-ring"><span>PLAN<br><b>FIRST</b></span></div></div><div class="product-copy"><span class="product-category">{{ $product->product_type }}</span><h3>{{ $product->name }}</h3><p>{{ $product->short_description }}</p><div class="product-meta"><span>View tool <b>↗</b></span><span>{{ str_replace('_', ' ', $product->availability) }}</span></div></div></a>@empty<div class="journal-empty"><div class="empty-index">TOOLS</div><div><h3>The first tools are being prepared.</h3><p>Product availability will appear here when ready.</p></div></div>@endforelse</div></div></section>
