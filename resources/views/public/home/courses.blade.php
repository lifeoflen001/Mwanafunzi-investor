<section class="section courses-section" id="courses"><div class="container"><div class="section-heading split-heading reveal"><div><p class="eyebrow"><span class="eyebrow-line"></span> {{ $section?->payload['eyebrow'] ?? 'Learn in the right order' }}</p><h2>{{ $section?->heading ?? 'Courses for the long game.' }}</h2></div><p class="body-copy">{{ $section?->body ?? 'Practical education for people who are ready to build a process — without profitability guarantees or shortcuts.' }}</p></div><div class="course-list">@forelse($courses as $course)<a class="course-row reveal" href="{{ route('courses.show', $course) }}"><span class="course-number">{{ str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</span><div class="course-name"><h3>{{ $course->title }}</h3><p>{{ $course->short_description }}</p></div><div class="course-detail"><span>{{ $course->level }}</span><span>{{ str_replace('_', ' ', $course->status) }}</span></div><span class="course-arrow" aria-hidden="true">↗</span></a>@empty<div class="journal-empty"><div class="empty-index">COURSES</div><div><h3>The first course is being prepared.</h3><p>Join the conversation to hear when enrolment opens.</p></div></div>@endforelse</div></div></section>
