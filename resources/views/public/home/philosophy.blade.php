@php
    $payload = $section->payload ?: [];
    $visualImage = \App\Support\PublicHero::resolve([$section->image, \App\Models\SiteSetting::getValue('hero_learn_image'), config('public.hero_defaults.learn')]);
    $pillars = $payload['cards'] ?? [];
@endphp
<section class="section philosophy" id="philosophy"><div class="container philosophy-grid"><div class="section-intro reveal"><p class="eyebrow"><span class="eyebrow-line"></span> {{ $payload['eyebrow'] ?? '' }}</p><h2>{{ $section->heading }}</h2><div class="body-copy rich-copy">{!! \App\Support\RichText::render($section->body) !!}</div>@if($section->cta_label && $section->cta_url)<a class="text-link" href="{{ $section->cta_url }}">{{ $section->cta_label }} <span aria-hidden="true">→</span></a>@endif</div><div class="philosophy-visual reveal reveal-delay-1"><div class="photo-card photo-card-back" aria-hidden="true"></div><div class="photo-card photo-card-main"><div class="desk-scene" @if($visualImage) style="background-image:linear-gradient(135deg,rgba(20,22,21,.12),rgba(20,22,21,.38)),url('{{ $visualImage['url'] }}');" @endif><span class="desk-sun"></span><span class="desk-paper paper-one"></span><span class="desk-paper paper-two"></span><span class="desk-pencil"></span></div><div class="visual-caption"><span>{{ $payload['visual_caption'] ?? '' }}</span><strong>{{ $payload['visual_index'] ?? '' }}</strong></div></div><div class="circle-note"><span>{{ $payload['visual_note'] ?? '' }}<br><em>{{ $payload['visual_note_emphasis'] ?? '' }}</em></span><i>↗</i></div></div></div>@if($pillars)<div class="container pillars reveal">@foreach($pillars as $pillar)<div class="pillar"><span>{{ $pillar['index'] ?? str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</span><strong>{{ $pillar['title'] ?? '' }}</strong><p>{{ $pillar['body'] ?? '' }}</p></div>@endforeach</div>@endif</section>
