@php($pillars = $section?->payload['cards'] ?? [])
<section class="section philosophy" id="philosophy"><div class="container philosophy-grid"><div class="section-intro reveal"><p class="eyebrow"><span class="eyebrow-line"></span> {{ $section?->payload['eyebrow'] ?? '' }}</p><h2>{{ $section?->heading ?? '' }}</h2><div class="body-copy rich-copy">{!! \App\Support\RichText::render($section?->body) !!}</div>@if($section?->cta_label && $section?->cta_url)<a class="text-link" href="{{ $section->cta_url }}">{{ $section->cta_label }} <span aria-hidden="true">→</span></a>@endif</div><div class="philosophy-visual reveal reveal-delay-1"><div class="photo-card photo-card-back" aria-hidden="true"></div><div class="photo-card photo-card-main"><div class="desk-scene"><span class="desk-sun"></span><span class="desk-paper paper-one"></span><span class="desk-paper paper-two"></span><span class="desk-pencil"></span></div><div class="visual-caption"><span>{{ $section?->payload['visual_caption'] ?? '' }}</span><strong>{{ $section?->payload['visual_index'] ?? '' }}</strong></div></div><div class="circle-note"><span>Always a<br><em>mwanafunzi.</em></span><i>↗</i></div></div></div>@if($pillars)<div class="container pillars reveal">@foreach($pillars as $pillar)<div class="pillar"><span>{{ $pillar['index'] ?? str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</span><strong>{{ $pillar['title'] ?? '' }}</strong><p>{{ $pillar['body'] ?? '' }}</p></div>@endforeach</div>@endif</section>
