@php
    $payload = $section->payload ?: [];
@endphp
<section class="section services-section" id="learn"><div class="container"><div class="section-heading split-heading reveal"><div><p class="eyebrow"><span class="eyebrow-line"></span> {{ $payload['eyebrow'] ?? '' }}</p><h2>{{ $section->heading }}</h2></div><p class="body-copy">{{ $section->body }}</p></div><div class="service-grid">@forelse($learningTopics as $topic)<a class="service-card reveal" href="{{ route('learn') }}"><div class="card-top"><span class="card-number">{{ str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</span><span class="card-icon" aria-hidden="true">{{ $topic->icon }}</span></div><h3>{{ $topic->title }}</h3><p>{{ $topic->short_description }}</p><span class="card-link">{{ $payload['card_cta_label'] ?? '' }} <span aria-hidden="true">↗</span></span></a>@empty<div class="journal-empty"><div class="empty-index">{{ $payload['empty_index'] ?? '' }}</div><div><h3>{{ $payload['empty_heading'] ?? '' }}</h3><p>{{ $payload['empty_body'] ?? '' }}</p></div>@if(!empty($payload['empty_cta_label']) && !empty($payload['empty_cta_url']))<a class="text-link" href="{{ $payload['empty_cta_url'] }}">{{ $payload['empty_cta_label'] }} <span aria-hidden="true">↗</span></a>@endif</div>@endforelse</div></div></section>
