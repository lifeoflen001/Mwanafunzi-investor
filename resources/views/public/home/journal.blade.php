@php
    $payload = $section?->payload ?? [];
@endphp
<section class="section journal-section" id="journal"><div class="container"><div class="section-heading split-heading reveal"><div><p class="eyebrow"><span class="eyebrow-line"></span> {{ $payload['eyebrow'] ?? 'Field notes, when there are field notes' }}</p><h2>{{ $section?->heading ?? 'Real Updates. Real Journey.' }}</h2></div><p class="body-copy">{{ $section?->body ?? 'The journal will be a living record of the work: reviews, lessons and ideas worth returning to. No invented headlines. No made-up results.' }}</p></div>@if($featuredArticle)<a class="journal-empty reveal" href="{{ route('journal.show', $featuredArticle) }}"><div class="empty-index">FEATURED</div><div><h3>{{ $featuredArticle->title }}</h3><p>{{ $featuredArticle->excerpt }}</p></div><span class="text-link">{{ $payload['card_cta_label'] ?? 'Read the note' }} <span aria-hidden="true">↗</span></span></a>@else<div class="journal-empty reveal"><div class="empty-index">{{ $payload['empty_index'] ?? 'J / 00' }}</div><div><h3>{{ $payload['empty_heading'] ?? 'The next entry is being studied.' }}</h3><p>{{ $payload['empty_body'] ?? 'When a new note is published, it will appear here from the connected journal source.' }}</p></div><a class="text-link" href="{{ $payload['empty_cta_url'] ?? route('contact') }}">{{ $payload['empty_cta_label'] ?? 'Ask for updates' }} <span aria-hidden="true">↗</span></a></div>@endif</div></section>
