@extends('layouts.public')
@section('title', 'Create an account — Mwanafunzi Investor')
@section('content')<section class="page-hero"><div class="container narrow"><p class="eyebrow">Customer portal</p><h1>Create your account.</h1><p class="lede">One account for purchases, downloads and course access.</p><form class="admin-form account-form" method="post" action="{{ route('register.store') }}">@csrf @include('partials.form-feedback')<label>Name<input name="name" value="{{ old('name') }}" required autocomplete="name"></label><label>Email<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label><div class="form-row"><label>Password<input type="password" name="password" required autocomplete="new-password"></label><label>Confirm password<input type="password" name="password_confirmation" required autocomplete="new-password"></label></div><div class="form-row"><label>Phone <span>(optional)</span><input name="phone" value="{{ old('phone') }}" autocomplete="tel"></label><label>Country code <span>(optional)</span><input name="country" value="{{ old('country') }}" maxlength="2" placeholder="TZ"></label></div><label class="consent"><input type="checkbox" name="terms" value="1" required> <span>I accept the <a href="{{ route('legal', 'terms') }}">Terms</a> and understand the <a href="{{ route('legal', 'risk-disclosure') }}">risk disclosure</a>.</span></label><button class="button button-dark" type="submit">Create account <span aria-hidden="true">↗</span></button><p>Already have an account? <a href="{{ route('login') }}">Sign in</a>.</p></form></div></section>@endsection
