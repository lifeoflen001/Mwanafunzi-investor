@extends('layouts.admin')
@section('title', $title)
@section('content')<x-admin.page-header eyebrow="{{ $eyebrow }}" title="{{ $title }}" description="Keep editorial classification clear and reusable." /><x-admin.card title="Add {{ $kind === 'categories' ? 'category' : 'tag' }}" class="admin-form-card"><form class="admin-form admin-inline-form" method="post" action="{{ route('admin.'. $kind . '.store') }}">@csrf<label>Name<input name="name" required></label><button class="button button-primary" type="submit">Add {{ $kind === 'categories' ? 'category' : 'tag' }} <span aria-hidden="true">+</span></button></form></x-admin.card><x-admin.card class="admin-table-card"><div class="admin-table"><div class="admin-table-head"><span>Name</span><span>Usage</span><span></span></div>@forelse($items as $item)<div class="admin-table-row"><div><strong>{{ $item->name }}</strong><small>{{ $item->slug }}</small></div><span>{{ $item->articles_count }} articles</span><x-admin.action-menu><form method="post" action="{{ route('admin.'. $kind . '.destroy', $item) }}">@csrf @method('delete')<button type="submit">Remove</button></form></x-admin.action-menu></div>@empty<x-admin.empty-state title="Nothing here yet." />@endforelse</div></x-admin.card>@endsection
