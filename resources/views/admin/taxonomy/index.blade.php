@extends('layouts.admin')
@section('title', $title)
@section('content')<div class="admin-heading"><div><p class="eyebrow">{{ $eyebrow }}</p><h1>{{ $title }}</h1><p>Keep editorial classification clear and reusable.</p></div></div><form class="admin-form admin-inline-form" method="post" action="{{ route('admin.'. $kind . '.store') }}">@csrf<label>Name<input name="name" required></label><button class="button button-dark" type="submit">Add {{ $kind === 'categories' ? 'category' : 'tag' }} <span aria-hidden="true">+</span></button></form><div class="admin-table"><div class="admin-table-head"><span>Name</span><span>Usage</span><span>Actions</span></div>@forelse($items as $item)<div class="admin-table-row"><div><strong>{{ $item->name }}</strong><small>{{ $item->slug }}</small></div><span>{{ $item->articles_count }} articles</span><div class="admin-actions"><form method="post" action="{{ route('admin.'. $kind . '.destroy', $item) }}">@csrf @method('delete')<button type="submit">Remove</button></form></div></div>@empty<div class="empty-state"><p>Nothing here yet.</p></div>@endforelse</div>@endsection
