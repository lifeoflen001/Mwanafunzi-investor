@extends('layouts.admin')
@section('title', 'Learning topics')
@section('content')<div class="admin-heading"><div><p class="eyebrow">CMS / Learn</p><h1>Learning topics</h1></div><a class="button button-dark" href="{{ route('admin.topics.create') }}">New topic <span aria-hidden="true">↗</span></a></div><div class="admin-table"><div class="admin-table-head"><span>Topic</span><span>Visibility</span><span>Actions</span></div>@foreach($topics as $topic)<div class="admin-table-row"><div><strong>{{ $topic->title }}</strong><small>{{ $topic->slug }}</small></div><span class="status-badge">{{ $topic->is_published ? 'published' : 'draft' }}</span><div class="admin-actions"><a href="{{ route('admin.topics.edit', $topic) }}">Edit</a></div></div>@endforeach</div>{{ $topics->links() }}@endsection
