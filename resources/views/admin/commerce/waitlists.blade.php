@extends('layouts.admin')
@section('title', 'Course waitlists')
@section('content')<div class="admin-heading"><div><p class="eyebrow">Commerce / Courses</p><h1>Waitlists</h1></div><a class="button button-light" href="{{ route('admin.commerce.waitlists.export') }}">Export CSV <span aria-hidden="true">↗</span></a></div><div class="admin-table"><div class="admin-table-head"><span>Course</span><span>Name</span><span>Email</span><span>Joined</span></div>@forelse($waitlists as $entry)<div class="admin-table-row"><span>{{ $entry->course->title }}</span><span>{{ $entry->name }}</span><span>{{ $entry->email }}</span><span>{{ $entry->joined_at->format('M j, Y H:i') }}</span></div>@empty<div class="empty-state"><p>No waitlist entries yet.</p></div>@endforelse</div>{{ $waitlists->links() }}@endsection
