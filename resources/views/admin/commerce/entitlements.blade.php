@extends('layouts.admin')
@section('title', 'Commerce entitlements')
@section('content')<div class="admin-heading"><div><p class="eyebrow">Commerce / Access</p><h1>Entitlements</h1></div></div><div class="admin-table"><div class="admin-table-head"><span>Customer</span><span>Resource</span><span>Order</span><span>Status</span></div>@forelse($entitlements as $entitlement)<div class="admin-table-row"><span>{{ $entitlement->user?->email ?: $entitlement->customer_email }}</span><span>{{ $entitlement->product?->name ?: $entitlement->course?->title }}</span><span>{{ $entitlement->order?->order_number ?: '—' }}</span><span>{{ $entitlement->status->value }}</span></div>@empty<div class="empty-state"><p>No entitlements yet.</p></div>@endforelse</div>{{ $entitlements->links() }}@endsection
