@extends('layouts.admin')
@section('title', 'Commerce payments')
@section('content')<div class="admin-heading"><div><p class="eyebrow">Commerce / Payments</p><h1>Payments</h1></div></div><div class="admin-table"><div class="admin-table-head"><span>Provider / reference</span><span>Order</span><span>Amount</span><span>Status</span></div>@forelse($payments as $payment)<div class="admin-table-row"><div><strong>{{ $payment->provider }}</strong><small>{{ $payment->internal_reference }}<br>{{ $payment->provider_transaction_id }}</small></div><span>{{ $payment->order->order_number }}</span><span>{{ app(\App\Services\MoneyFormatter::class)->format($payment->amount, $payment->currency) }}</span><span>{{ $payment->status->value }}<small>{{ $payment->verified_at?->format('M j, Y H:i') }}</small></span></div>@empty<div class="empty-state"><p>No payments yet.</p></div>@endforelse</div>{{ $payments->links() }}@endsection
