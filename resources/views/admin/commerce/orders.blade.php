@extends('layouts.admin')
@section('title', 'Commerce orders')
@section('content')<div class="admin-heading"><div><p class="eyebrow">Commerce / Orders</p><h1>Orders</h1></div><a class="back-link" href="{{ route('admin.commerce.dashboard') }}">← Commerce overview</a></div><form class="admin-filters" method="get"><input name="q" value="{{ request('q') }}" placeholder="Search order or customer" aria-label="Search orders"><select name="status"><option value="">All statuses</option>@foreach(['pending_payment','processing','paid','completed','cancelled','refunded','partially_refunded'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ str_replace('_', ' ', $status) }}</option>@endforeach</select><button class="button button-light" type="submit">Filter</button></form><div class="admin-table"><div class="admin-table-head"><span>Order</span><span>Customer</span><span>Total</span><span>Status</span></div>@forelse($orders as $order)<a class="admin-table-row" href="{{ route('admin.commerce.orders.show', $order) }}"><strong>{{ $order->order_number }}<small>{{ $order->items_count }} item(s)</small></strong><span>{{ $order->customer_name }}<small>{{ $order->customer_email }}</small></span><span>{{ app(\App\Services\MoneyFormatter::class)->format($order->total, $order->currency) }}</span><span>{{ str_replace('_', ' ', $order->status->value) }}<small>{{ str_replace('_', ' ', $order->payment_status->value) }}</small></span></a>@empty<div class="empty-state"><p>No orders yet.</p></div>@endforelse</div>{{ $orders->links() }}@endsection
